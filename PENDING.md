# PENDING — nightly docs sync

Regenerated by `/sync-docs` (nightly mode). Cumulative window: parent release `v2026.618.0` → `master` HEAD `841742fc`. This run's new authoring scope is the delta since the last run (`51ffbb38` → `841742fc`, 9 commits).

This file is overwritten each run — it always reflects the current run, not an append log.

## Applied this run (committed to `nightly`)

**Nothing authored.** Of the 9 new parent commits since the last run, only one (`50ae8fc6`) clears the 24h quarantine, and it is a non-doc-worthy UX refinement (see below). The other 8 are younger than 24h and are held back so any reverts can land before we document them — they become eligible on the next run.

## Quarantined (eligible next run, < 24h old at this run)

Cutoff this run: `2026-06-24T20:24:06Z` (now − 24h). These cleared into the window but are deferred:

| Parent commit | Surface | Likely doc target | Tier |
|---|---|---|---|
| `1951c802` feat(cli): surface plugin install target host + add `plugin target` (#8575) | **New CLI command `paperclipai plugin target`** + install-target host surfacing | `docs/reference/cli/` (plugin), `docs/cli/control-plane-commands.md` | pr |
| `841742fc` Graduate experimental conference room defaults (#8628) | Experimental "conference room" defaults graduated to default-on | `docs/guides/board-operator/experimental-features.md`, `docs/administration/settings.md` | pr |
| `ed65d08d` Gate skill mutations with `skills:create` permission (#8616) | New `skills:create` permission key gating skill mutation | `docs/reference/skills.md`, `docs/guides/org/skills.md`, agent-permissions docs | pr |
| `b4a7efa8` Add skill category editing in settings (#8615) | Edit a skill's category from company settings | `docs/guides/org/skills.md`, `docs/reference/skills.md` | pr (+ screenshot) |
| `bac15ebd` feat: task status icons & colors (#8580) | Reworked task status icons / colour glyphs (`StatusGlyph`, `StatusIcon`, `status-colors`) | `docs/user-guides/**` (status legend), screenshots | pr (+ screenshot) |
| `a27e5ad0` Add ephemeral sandbox runtime status plumbing (#8593) | Ephemeral sandbox runtime status (server + shared types) | `docs/reference/adapters/sandbox-providers.md` (if user-visible) | pr |
| `800dab1c` Render sandbox runtime status in issue threads (#8594) | Sandbox runtime status shown in issue threads | `docs/guides/day-to-day/issues.md`, screenshots | pr (+ screenshot) |
| `721541c4` Fix sandbox restore index drift (#8595) | Internal adapter-utils bugfix | — (no user-facing impact expected) | context-only |

## Not authored this run (eligible, but no doc impact)

- **`50ae8fc6` Improve reusable workspace selector search (#8597)** — adds a `SearchableSelect` component and wires search into the existing reusable-execution-workspace picker in the New Issue dialog. Pure UX refinement of an existing selector; no new CLI/API/env/schema surface, no documented behaviour change. No registry screenshot has a `depends_on` on the touched files (`NewIssueDialog.tsx`, `IssueWorkspaceCard.tsx`, `ReusableExecutionWorkspaceSelect.tsx`), so no screenshot staleness either. Left unauthored.

## ⚠ Drift

None. `check-drift.mjs --against master` reported 0 drift records — every documented parent path, CLI command, env var, and REST route still resolves upstream.

## ⚠ Reconcile

None. No previously-applied surface was reverted upstream; all five surfaces authored in the prior run (`51ffbb38`) remain present in the cumulative diff.

## Screenshots

See `SCREENSHOTS_PENDING.md` — **98 screenshots** depend on UI files changed across the cumulative window (release tag → HEAD). None are newly driven by this run's single eligible commit; the count grew from 60 because the cumulative window now spans back to the release tag. Recapture with `npm run screenshots:refresh` (requires a running instance; PNGs land in a PR for review — never auto-pushed). Several quarantined commits above will add further status-icon / issue-thread staleness once they clear next run.

## ⚠ Pre-existing build/lint warnings (not introduced by this run)

- **`sync:lint-links` reports 1 "broken internal link"** in `docs/reference/skills/optional/research/last30days.md` (`-> url`) — a false positive: the checker parses ``[name](url)`` inside an inline-code span (prose documenting markdown syntax) as a real link. Fix belongs in `scripts/sync/lint-links.mjs` (skip inline-code spans) or the example doc — outside this sync's scope.
