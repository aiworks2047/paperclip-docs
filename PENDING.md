# PENDING — nightly docs sync

Regenerated by `/sync-docs` (nightly mode). Cumulative window: parent release `v2026.618.0` → `master` HEAD `51ffbb38`. This run's new authoring scope is the delta since the last run (`9ac24317` → `51ffbb38`, 29 commits).

This file is overwritten each run — it always reflects the current run, not an append log.

## Applied this run (committed to `nightly`)

All edits below were authored from parent code on `master` and verified with `verify-edit.mjs` — **0 unverified, 0 suspicious** across all five files.

| Surface | Doc | Parent source |
|---|---|---|
| **External object references** (GitHub PR/issue status-aware pills across issue surfaces, filters, inbox/sidebar badges) | `docs/guides/day-to-day/issues.md` | PR #8512; `server/src/services/github-external-object-provider.ts`, `packages/shared/src/external-objects.ts`, `ui/src/lib/external-objects.ts`, `ui/src/lib/issue-filters.ts` |
| **Streamlined left navigation now default-on** (classic sidebar is the opt-out) | `docs/administration/settings.md` | PR #8496; `ui/src/components/Sidebar.tsx`, `ui/src/pages/InstanceExperimentalSettings.tsx` |
| **Daytona reusable leases + quota-safe auto-archive + image-only resource overrides** | `docs/reference/adapters/sandbox-providers.md` | PRs #8513, #8561, #8564; `packages/plugins/sandbox-providers/daytona/src/{manifest,plugin}.ts` |
| **Routine description annotations** (comment on / inline-edit routine instructions like an issue document) | `docs/guides/projects-workflow/routines.md` | PR #8229; `server/src/services/routines.ts`, `ui/src/components/routine-sections/editable-sections.tsx`, `ui/src/pages/RoutineDetail.tsx` |
| **Plugin SDK external-object reference provider surface** (detect → resolve → refresh lifecycle, declaration + protocol types) | `docs/reference/plugins/sdk.md` | PR #8512; `packages/plugins/sdk/src/{types,protocol,define-plugin,index}.ts` |

## ⚠ Reconcile

- **`skills/paperclip-dev` removed upstream with no rename target** (detect-renames `removed_dirs_no_match`). Already handled in the prior nightly run (commit `2ff5539` "paperclip-dev cleanup"). No further action expected — flagged for confirmation only.

## ⚠ Build/lint warnings (not introduced by this run)

- **`sync:lint-links` reports 1 "broken internal link"** in `docs/reference/skills/optional/research/last30days.md` (`-> url`). This is a **false positive**: the checker parses the literal text `` `[name](url)` `` *inside an inline-code span* (prose documenting markdown link syntax) as a real link. The file arrived via the `main` merge (commit `5b4b489` "Add Skills docs hierarchy"), so the same warning fires on `main`. Fix belongs in `scripts/sync/lint-links.mjs` (skip inline-code spans) or the example doc — both outside this sync's scope.
- **`verify-edit` flagged 10 "unverified env-var" tokens in `sdk.md`** (`PLUGIN_CATEGORIES`, `PLUGIN_CAPABILITIES`, …). False positives — these are pre-existing exported SDK constants listed at `sdk.md:246`, not env vars, and were not touched by this run.

## Screenshots

See `SCREENSHOTS_PENDING.md` — **60 screenshots** depend on UI files changed this window (External Objects pills, streamlined sidebar, experimental settings: `Inbox.tsx`, `IssueDetail.tsx`, `CompanySkills.tsx`, `AgentConfigForm.tsx`, `InstanceExperimentalSettings.tsx`, `RunTranscriptView.tsx`). Recapture with `npm run screenshots:refresh` (requires a running instance; PNGs land in a PR for review — never auto-pushed).

## Not authored (out of scope / context-only this run)

- **Plan→task dependency clarification** (PR #8485, parent `skills/paperclip-converting-plans-to-tasks/SKILL.md`) — skill-text guidance about encoding hard dependencies as `blockedByIssueIds`. No clean 1:1 target in our docs; the concept is already covered by `docs/guides/day-to-day/blocked-inbox.md` and `issues.md`. Left as context.
- Dependency bumps, CI/workflow changes, test-only additions, and pure refactors in the window — no user-facing doc impact.
