# PENDING — nightly sync manifest

_Regenerated by `/sync-docs` on 2026-06-22 (nightly mode). Cumulative window: parent `v2026.618.0` → `d1e6662e` (HEAD `9ac2431` quarantined, <24h old). 64 commits, 303 changed files._

This file is overwritten each run — it always reflects the current cumulative manifest, not an append log.

## ✅ Applied to `nightly` this run (verified against parent)

| Surface | Doc target | Change |
|---|---|---|
| codex-local: inactivity monitor | `reference/adapters/codex-local.md` | New `outputInactivityTimeoutMs` config field (default 7 min / `420000`, `null` disables) |
| codex-local: host login inheritance | `reference/adapters/codex-local.md` | New "Authentication And Per-Agent Homes" section — agents inherit host Codex login by default; per-agent home seeding; fail-fast on no creds |
| gemini-local: execution model | `reference/adapters/gemini-local.md` | Note now states `--prompt` (not stdin) + headless-safe terminal/browser env |
| CLI `auth login --no-browser` | `reference/cli/authentication.md` | New flag: print approval URL instead of opening a browser |
| Skills: `required` attribute retired | `reference/cli/skills.md`, `reference/skills.md` | Dropped "non-required" qualifiers; removed `required`/`requiredReason` snapshot fields + `paperclip_required` origin; corrected "bundled skills forced into desired set" → exact desired-set resolution; UI grouping is now Company skills + Unmanaged (no "Required" group); removed retired `required` frontmatter row |
| `paperclip-dev` skill removed | `reference/skills.md` | Removed from bundled list; replaced stale `paperclip-dev` source/key examples with `paperclip-create-agent` |
| Experimental features caution | `administration/settings.md` | Strengthened to match the new in-app warning (no compatibility guarantees; may break/change/be removed) |
| **Task watchdogs (new feature)** | `guides/projects-workflow/task-watchdogs.md` **(new page)** + `site/content.json` nav | New operator guide: what it is, when it wakes, how to set one (UI + `GET/PUT/DELETE /api/issues/:id/watchdog`), writing instructions, hard limits, when-not-to-use |

Verification (verify-edit against `master`): codex-local 38/40, gemini-local 27/28, authentication 37/37, reference/skills 7/7, cli/skills 37/37 — all clean. The only unverified records (`my-gateway`, `secret-id`) are pre-existing example placeholders, not this run's edits. Build ✓, links ✓, nav ✓ (133 entries), drift ✓.

## ✓ Reviewed — no doc change needed

- **Instance-scoped execution environments** (#8375 `547463d3`). Checked every documented environment route in `reference/api/instance-admin.md` against current `routes/environments.ts` — all still present and correct (including `POST .../environments/probe-config`). No doc prose claims company-scoping. Impact is UI/storage only → handled by screenshot refresh below.
- **Skills store** (#skills-catalog). Our `guides/org/skills.md` describes the Skills page functionally and accurately. "Skills Store" is a UI nav reframe of the existing catalog + company library; no rendered heading cleanly confirmed, so no terminology edit forced — screenshot recapture will surface any naming.
- **Plugin SDK lease params** — `protocol.ts` added `agentId` / `executionWorkspaceId?` to `PluginEnvironmentAcquireLeaseParams`. `reference/plugins/sdk.md` only lists the type name, not its fields, so nothing to update.

## ⚠ Drift (Phase 1.5)

None. Drift checker against `master` returned 0 records.

## ↻ Reconciliation (Phase 3.5)

None.

## 📸 Screenshots

80 of 276 registry entries are stale (heavy UI churn in `ui/src/**`, incl. agents, issues, routines, environments, experimental settings). See `SCREENSHOTS_PENDING.md`. Run `npm run screenshots:refresh` and review the resulting PR. Task-watchdog UI surfaces (issue properties / new-issue dialog) are not yet in the screenshot registry — add captures when the feature stabilizes.
