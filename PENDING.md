# PENDING — nightly sync manifest

_Regenerated by `/sync-docs` on 2026-06-22 (nightly mode). Cumulative window: parent `v2026.618.0` → `d1e6662e` (HEAD `9ac2431` quarantined, <24h old). 64 commits, 303 changed files._

This file is overwritten each run — it always reflects the current cumulative manifest, not an append log.

## ✅ Applied to `nightly` this run (PR-tier, verified against parent)

| Surface | Doc target | Change | Parent evidence |
|---|---|---|---|
| codex-local: inactivity monitor | `docs/reference/adapters/codex-local.md` | New `outputInactivityTimeoutMs` config field (default 7 min, `null` disables) | `packages/adapters/codex-local/src/index.ts`, `output-inactivity-monitor.ts` (#5017) |
| codex-local: host login inheritance | `docs/reference/adapters/codex-local.md` | New "Authentication And Per-Agent Homes" section — agents inherit host Codex login by default; per-agent home seeding; fail-fast on no creds | `codex-home.ts`, commit `d1e6662e` (#8425) |
| gemini-local: execution model | `docs/reference/adapters/gemini-local.md` | Note now states `--prompt` (not stdin) + headless-safe terminal/browser env for unattended runs | `packages/adapters/gemini-local/src/index.ts`, `execute.ts` |
| CLI `auth login --no-browser` | `docs/reference/cli/authentication.md` | New flag: print approval URL instead of opening a browser | `cli/src/commands/client/auth.ts` |

Verification: codex-local 38/40, gemini-local 27/28, authentication 37/37 claims verified. The 2 unverified records (`my-gateway`, `secret-id`) are pre-existing illustrative example placeholders, not this run's edits.

## ⚠ Surfaced for human review (NOT auto-applied)

These are user-visible but need judgement, touch multiple files, involve a removal, or need a new doc home + screenshots. Decide and draft manually.

1. **Skills: "required skill attribute" retired** (#7029, `33353ce` + `skills.ts`, `shared/src/types/adapter-skills.ts -3`, `validators/adapter-skills.ts -3`).
   - The per-agent *desired-skill* `required` attribute was removed; CLI `skills agent sync/clear` descriptions dropped the "non-required" qualifier and the snapshot no longer prints a `required` column. Bundled/server-enforced (`paperclip_required`) skills still exist.
   - Affected docs (currently describe `required`/`non-required`/server-enforced wording): `docs/reference/cli/skills.md` (lines ~20, 23, 165, 175–177, 181), `docs/reference/skills.md` (lines ~54, 326, 336, 352–393).
   - **Why not auto:** semantic removal — risk of mis-documenting which "required" notion survived. Verify against `packages/shared/src/validators/adapter-skills.ts` before editing.

2. **`skills/paperclip-dev/SKILL.md` removed** (#7029). The bundled `paperclip-dev` skill was deleted from parent. Drift checker found no `.ts/.mjs` refs, but `paperclip-dev` is named in: `docs/reference/cli/common-options.md`, `setup-commands.md`, `installation.md`, `deploy/environment-variables.md`, `reference/skills.md`. Confirm whether these references should be removed/updated.

3. **Execution environments are now instance-scoped** (#8375 `547463d3`, migration `0105_instance_scoped_environments.sql`). Large UI rewrite (`CompanyEnvironments.tsx` +453/-396), routes (`environments.ts`, `instance-settings.ts`), services. Environments moved from company-scoped to instance-scoped; create/edit moved into a dialog (#8391); adapter support matrix table removed (#8398). → Needs review of any environments guide/CLI page + screenshot refresh.

4. **Task watchdogs** — major new feature. New `server/src/services/task-watchdogs.ts` (+1565), `task-watchdog-scope.ts`, routes in `issues.ts` (+801), migration `0104_issue_watchdogs.sql`, schema `issue_watchdogs.ts`, UI `task-watchdog-surfaces.stories.tsx`. Parent added internal `doc/TASK-WATCHDOG.md`. → No doc home yet; decide whether/where to document for operators.

5. **Experimental features (operator-facing)** (#8382 `fce3b439`). New `InstanceExperimentalSettings.tsx` warning + CLI surface `paperclipai instance settings:experimental[:update]` and `settings:general[:update]`. Parent added `docs/guides/board-operator/experimental-features.md` and a control-plane CLI section. → Consider adding the CLI commands to the reference + a short "experimental features" operator note. Screenshot `settings/experimental.png` is stale.

6. **Skills store** — parent added `docs/guides/agent-developer/skills-store.md`; `server/src/services/skills-catalog.ts` (+126/-15), `company-skills`. → Review whether `docs/reference/skills.md` / `docs/guides/org/skills.md` need a "skills store" framing update.

7. **Plugin SDK lease params** — `packages/plugins/sdk/src/protocol.ts` added `agentId` and `executionWorkspaceId?` to `PluginEnvironmentAcquireLeaseParams`. Minor; relevant to environment-driver plugin authors. → Optional touch-up to `docs/reference/plugins/sdk.md`.

## ⚠ Drift (Phase 1.5)

None. Drift checker against `master` returned 0 records.

## ↻ Reconciliation (Phase 3.5)

None. No previously-applied manifest entries disappeared (prior state carried no `last_applied_manifest_hash`).

## 📸 Screenshots

80 of 276 registry entries are stale (heavy UI churn in `ui/src/**`). See `SCREENSHOTS_PENDING.md`. Run `npm run screenshots:refresh` and review the resulting PR.
