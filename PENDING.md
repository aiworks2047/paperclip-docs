# PENDING — nightly sync manifest

_Regenerated by `/sync-docs` on 2026-06-11T10:26Z (nightly mode)._

- **Window:** `v2026.609.0` (`a0f7d3d`) → parent `master` HEAD (`69a368e`) — 27 commits, 262 files.
- **Mode:** nightly. **Drift:** 0. **Auto-merge (env-var) changes:** none.
- **Quarantine cutoff:** `2026-06-10T10:26Z` (24h). Everything that landed after the cutoff is held for the next run so reverts have time to settle.

---

## ✍️ Drafted this run (PR tier — safe window, on `nightly`)

| Surface | Parent | Target docs |
|---|---|---|
| Workspace file viewer & inline artifact links | #7681 (`468edd8b`) | `docs/guides/day-to-day/artifacts.md` |
| Routines: human-readable cron, trigger cards, run-history display, variation-C layout | #7848, #7858 (`bf62e3fb`, `fae7e920`) | `docs/guides/projects-workflow/routines.md` |
| Interrupt handoffs & scoped wake semantics | #7855 (`67b22d87`) | `docs/guides/day-to-day/issues.md` |

## 📝 Noted, not drafted (minor / needs human eyes)

- **Feedback item in account flyout menu** (#7854, `e3aada1d`) — small UI addition (`SidebarAccountMenu`). Consider a one-line note in `docs/guides/day-to-day/feedback-voting.md`.
- Reliability bugfixes with no user-visible doc surface (context-only): claude-local poisoned `previous_message_id` recovery (#5972), defer same-issue `forceFreshSession` wakes (#4080), validate session ID as UUID before `--resume` (#1742), heartbeat sessionId reuse across adapter swap (#4109).

---

## 🔒 Quarantined — held until next run (landed < 24h ago)

These are the big ones. They will enter the manifest on the next nightly run once past the 24h window:

- **Kubernetes sandbox provider** — self-hostable K8s sandbox provider plugin + server execution integration + agent-runtime harness images (stages 1–3: #5790, #7938, #7934). New `packages/plugins/sandbox-providers/kubernetes/**`, `server/src/services/adapter-registry-bootstrap.ts`, `execution-allowlist`, `execution-policy-bootstrap`, `ui/src/lib/forced-kubernetes-environment.ts`, `docker/agent-runtime/**`. **Major new surface** — likely a new page under `docs/reference/adapters/` (sandbox-providers) + execution-policy guide updates.
- **Env-driven gateway routing** for `opencode-local` (#7837), `pi-local` (#7920), `codex-local` (#7919) — custom providers / small-cheap model / remote allow-all via `PAPERCLIP_*_PROVIDERS`. Touches the four adapter reference pages.
- **GPT-5.5 added to Codex local model options** (#5575); codex-local omit-default-model behavior (#7971).
- **gemini-local** auth pre-select for headless runs (#7918); gemini token-overflow fresh-session (#4932).
- Assorted codex-local auth.json symlink / EEXIST race fixes (#5028, #5269); sandbox workspace tar fix (#7836); keep agent follow-ups in run workspace.

---

## 📸 Screenshot staleness

See `SCREENSHOTS_PENDING.md`. Routines and issue-detail captures depend on UI paths that changed in this window.
